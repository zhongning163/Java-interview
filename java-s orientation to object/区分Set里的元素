Set里的元素是不能重复的，用什么方法来区分重复与否？是用==还是equals（）？它们有何区别？用contains来区分是否有重复的对象，还是都不用

在比较是先调用hashCode方法，如果不相同，证明不相等
如果相同，再调用equals方法，如果euqals方法返回true，证明相等，否则证明不相等

==：主要用在基本数据类型与引用
euqals：主要是对象或对象引用的比较

集合中是否包含某一个元素用contains来判断
