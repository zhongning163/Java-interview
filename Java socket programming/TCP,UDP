TCP、UDP区别
1、发送数据前是否存在建立连接过程，TCP面向连接，效率会稍低，UDP面向非连接，速度更快
2、TCP通过确认机制，丢包可以重发，保证数据的正确性，UDP不保证正确性，只是单纯的负责发送数据包
3、UDP是面向报文的，发送方的UDP对应用程序叫来的报文，在添加首部之后就向下交付给IP层，既不拆分也不合并，而是保留报文的边界，因此应用程需要选择合适的报文大小
4、UDP头部只有8个字节，UDP每个数据报的大小限制在64K内，TCP头部20个字节，在连接中进行大数据量传输
